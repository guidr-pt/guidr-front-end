const initialState = {
  user: {},
  activeTrip: {},
  trips: [],
  recentTrips: []
}

export const appReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}
